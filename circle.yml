machine:
  node:
    version:  5.1.1

dependencies:
  override:
    - sudo pip install awscli
    - npm install

deployment:
  staging:
    branch: master
    commands:
    - ./deploy_staging.sh
  production:
    branch: production
    commands:
    - ./deploy_production.sh
