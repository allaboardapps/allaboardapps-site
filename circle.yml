machine:
  node:
    version:  5.1.1

dependencies:
  override:
    - sudo pip install awscli

test:
  override:

deployment:
  staging:
    branch: master
    commands:
    - ./sh/deploy_staging.sh
  production:
    branch: master
    commands:
    - ./sh/deploy_production.sh
