machine:
  node:
    version:  5.1.1

dependencies:
  override:
    - npm install
    - bundle install
    - sudo pip install awscli

    cache_directories:
    - "node_modules"

deployment:
  aws:
    branch: master
    commands:
    - ./sh/deploy_staging.sh
